<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:valueConverters="clr-namespace:SampleApp.Common.ValueConverters;assembly=SampleApp"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SampleApp.Views.CharacteristicDetailPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <valueConverters:GuidToStringValueConverter x:Key="GuidToStringValueConverter" />
            <valueConverters:CharactersticPropertiesToStringConverter x:Key="ChractersticPropertiesToStringConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <Label Text="{Binding Uuid, Converter={StaticResource GuidToStringValueConverter}}" />
        <Label Text="{Binding Properties, Converter={StaticResource ChractersticPropertiesToStringConverter}}" />
      <Label Text="{Binding Value}" />
        <StackLayout Orientation="Horizontal">
            <Button Text="Read Value" IsEnabled="{Binding CanRead}" Command="{Binding ReadValueCommand}"/>
            <Button Text="Enable notifications" IsEnabled="{Binding CanNotify}" />
        </StackLayout>

        <Button Text="Write Value" IsEnabled="{Binding CanWrite}" />
    </StackLayout>
</ContentPage>